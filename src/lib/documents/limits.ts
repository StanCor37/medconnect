/**
 * Hardcoded this phase — spec §6's per-Case configurability needs an
 * admin-config surface that doesn't exist yet (Segment 9's job).
 */
export const MAX_FILE_SIZE_BYTES = 25 * 1024 * 1024; // 25 MB
export const MAX_FILES_PER_UPLOAD = 20;
export const MAX_TOTAL_UPLOAD_BYTES = 150 * 1024 * 1024; // 150 MB
